version: 2
jobs:
  build:
    branches:
      only:
        - circleci
    docker:
      - image: circleci/node:11.10.1
    steps:
      - checkout
      - run: #install yarn
          name: install-yarn
          command: sudo apt install yarn
      - run: #get dependencies
          name: get-dependencies
          command: yarn
      - run: #run tests
          name: start-server
          command: yarn start & yarn test && yarn stop